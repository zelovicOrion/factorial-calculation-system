# factorial-calculation-system
The objective is to develop a system consisting of two interacting applications: a Client Module (validation and routing) and a Server Module (computation and persistence). Communication between the modules must be implemented via the HTTP (REST) protocol.
